# 更新日志

本文件记录 `astrbot_plugin_life_companion` 的重要变更。

## v1.0.2 - 2026-09-04

### 修复

- `今日生活照` 只调用 `astrbot_plugin_life_companion_image`，移除旧生图插件和旧配置文件的运行时回退路径。

## v1.0.1 - 2026-09-04

### 修复

- `今日生活照` 优先调用 `astrbot_plugin_life_companion_image`，并保留旧版 `astrbot_plugin_gitee_aiimg` 兼容入口。
- 移除固定的“正在根据今日日程生成生活照”提示，交由生图插件根据当前人设、对话上下文和用户语气生成自然承接话。

## v1.0.0 - 2026-09-02

### 新增

- 维护连续的每日生活状态，结合日期、节日、历史日程和近期对话生成穿搭、时间线及生活照提示词。
- 支持按早上、中午、下午和晚上提供分时段穿搭，并向对话与生图插件提供当前状态。
- 支持日程查看、重写、历史版本、回滚、旧数据导入和参考会话管理。
- 提供严格只读的 `get_life_context(allow_generate=False)` 接口，避免生图插件读取状态时额外触发 LLM。

## 2026-09-04

- 新增早上、中午、下午、晚上的四个时段的穿搭数据。
- 根据当前时间向生活状态与生图上下文返回对应时段穿搭，同时保留完整四段穿搭信息。
- 更新日程生成、缓存数据与 `image_prompt` 的上下文适配，并补充相关测试与配置说明。

## 2026-09-01

- 初始化仓库，并完成独立的 Life Companion AstrBot 插件初版。
- 支持按日期、节日、人格、历史日程和近期会话生成结构化的每日生活状态与时间线。
- 提供日程查看、重写、历史版本、回滚、旧数据导入和自动生成时间等管理能力。
- 增加 `今日生活照` 命令及与 `astrbot_plugin_gitee_aiimg` 的生活上下文集成。
- 提供独立数据目录、生成失败保护和只读生活上下文接口。
